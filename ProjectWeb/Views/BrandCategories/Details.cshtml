@model ProjectWeb.Data.BrandCategories

@{
    ViewData["Title"] = "Details";
}

<section class="contact spad">
    <div class="container">
        <div class="row">
            <h1>Details</h1>
        </div>
        <dl class="row mt-4">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.BrandName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.BrandName)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.BrandDescription)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.BrandDescription)
            </dd>
        </dl>
        <div class="row mt-5">
            <a asp-action="Edit" asp-route-id="@Model?.BrandID" class="btn btn-success">Edit</a> |
            <a asp-action="Index" class="btn btn-success">Back to List</a>
        </div>
    </div>
</section>