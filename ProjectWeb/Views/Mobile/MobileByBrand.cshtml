@model ProjectWeb.Models.MyViewModel

<section class="featured spad">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="section-title">
                    <h2>@Model.BrandName</h2>
                </div>
                @*<div class="featured__controls">
                    <ul>
                        <li class="active" data-filter="*">All</li>
                        @foreach (var brand in Model.listBrands)
                        {
                            <li data-filter=".Smartphone-@brand.BrandName">@brand.BrandName</li>
                        }
                    </ul>
                </div>*@
            </div>
        </div>
        <div class="row featured__filter">
            @foreach (var mobile in Model.listMobileByBrand)
            {
                <partial name="~/Views/Shared/_ProductPartial.cshtml" model="@mobile" />
            }
        </div>
    </div>
</section>

@section Scripts {
    <script>
        $(document).ready(function () {
            $(".addtocart").click(function (event) {
                event.preventDefault();
                var id = $(this).attr("data-id");
                console.log(id)
                $.ajax({
                    type: "POST",
                    url: "@Url.Content("~/ShoppingCart/AddToCart")",
                    data: {
                        id: id,
                    },
                    success: function (result) {

                    }
                });
            });
        });
    </script>
}